import styled from "@emotion/styled/macro";
import * as colors from "../../styles/colors";

const FormErrorMessage = styled.small({
  fontSize: ".75rem",
  color: colors.danger,
});

export default FormErrorMessage;
